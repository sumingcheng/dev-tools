version: '3.8'
services:
  frpc:
    image: fatedier/frpc:v0.55.1
    container_name: frpc
    volumes:
      - ./frpc.ini:/etc/frp/frpc.ini
    restart: always
    command: /usr/bin/frpc -c /etc/frp/frpc.ini
